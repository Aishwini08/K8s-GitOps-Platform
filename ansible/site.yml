---
- name: Configure EKS Worker Nodes
  hosts: eks_workers
  become: true
  remote_user: ec2-user

  roles:
    - os-hardening
    - node-setup
    - containerd
    - node-exporter
    - prometheus-config
    - alertmanager
